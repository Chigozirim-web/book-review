
module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset'
  ],
};

/* 
const path = require(`path`);

configureWebpack: {
    resolve: {
      symlinks: false,
      alias: {
        vue: path.resolve(`./node_modules/vue`)
      }
    }
  }
*/